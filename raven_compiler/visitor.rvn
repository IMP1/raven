class Visitable {

    def accept (Visitor visitor) {
        visitor.visit(this)
    }
}


abstract class Visitor {

    def visit (Visitable subject) {
        '' Override this with more specific parameter types.
    }

}
